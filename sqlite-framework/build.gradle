plugins {
    id("com.android.library")
    id "com.vanniktech.maven.publish"
}

android {
    compileSdk 30
}

dependencies {
    api(project(":sqlite"))

    api("androidx.annotation:annotation:1.0.0")
}
